{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$file_path\" ] && [[ \"$file_path\" == *.sh ]]; then bash -n \"$file_path\" 2>&1 || true; fi"
          }
        ]
      }
    ]
  }
}
