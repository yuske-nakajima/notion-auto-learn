---
paths:
  - "test/**/*.sh"
---
# テスト開発ルール

- テスト関数は `assert_eq` ヘルパーを使い、説明・期待値・実測値の 3 引数で呼ぶ
- テストは `PASS` / `FAIL` カウントを管理し、最後に結果サマリーを出力する
- 失敗時は `exit 1` で終了する
- テストファイルも `set -euo pipefail` を先頭に記述する
- 既存のテストパターン (`test-*.sh`) に命名を合わせる
